<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context"
       xmlns:aop="http://www.springframework.org/schema/aop" xmlns:tx="http://www.springframework.org/schema/tx"
       xsi:schemaLocation="
http://www.springframework.org/schema/beans
http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
http://www.springframework.org/schema/context
http://www.springframework.org/schema/context/spring-context-3.0.xsd
http://www.springframework.org/schema/tx
http://www.springframework.org/schema/tx/spring-tx-3.0.xsd
http://www.springframework.org/schema/aop
http://www.springframework.org/schema/aop/spring-aop-3.0.xsd">

    <bean id="gb.placeholderConfig"
          class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
        <property name="location" value="classpath:/datasource.properties" />
    </bean>


    <context:annotation-config />

    <context:component-scan base-package="gambler.commons.auth"/>

    <bean id="dataSource" class="org.apache.commons.dbcp.BasicDataSource"
          destroy-method="close">
        <property name="driverClassName" value="${gb.datasource.driverClassName}" />
        <property name="url" value="${gb.datasource.url}" />
        <property name="username" value="${gb.datasource.userName}" />
        <property name="password" value="${gb.datasource.password}" />
        <property name="maxActive" value="${gb.datasource.maxActive}" />
        <property name="maxIdle" value="${gb.datasource.maxIdle}" />
        <property name="maxWait" value="${gb.datasource.maxWait}" />
        <property name="initialSize" value="${gb.datasource.initialSize}" />
        <property name="removeAbandoned" value="${gb.datasource.removeAbandoned}" />
        <property name="removeAbandonedTimeout" value="${gb.datasource.removeAbandonedTimeout}" />
        <property name="logAbandoned" value="${gb.datasource.logAbandoned}" />
    </bean>

    <bean id="sessionFactory"
          class="org.springframework.orm.hibernate3.LocalSessionFactoryBean">
        <property name="dataSource">
            <ref local="dataSource" />
        </property>
        <property name="mappingResources">
            <list>
                <value>
                    AuthDomains.hbm.xml
                </value>
            </list>
        </property>
        <property name="hibernateProperties">
            <props>
                <prop key="hibernate.dialect">
                    ${gb.datasource.dialect}
                </prop>
                <prop key="hibernate.show_sql">
                    ${hibernate.show_sql}
                </prop>
                <prop key="hibernate.hbm2ddl.auto">
                    ${hibernate.hbm2ddl.auto}
                </prop>
            </props>
        </property>
    </bean>

    <bean id="simpleHibernatePersistence"
          class="gambler.commons.persistence.hibernate.SimpleHibernatePersistence">
        <property name="sessionFactory">
            <ref local="sessionFactory" />
        </property>
    </bean>

    <tx:annotation-driven transaction-manager="transactionManager" />

    <bean id="transactionManager"
          class="org.springframework.orm.hibernate3.HibernateTransactionManager">
        <property name="sessionFactory">
            <ref local="sessionFactory" />
        </property>
    </bean>
    
    <bean id="auth-service" class="gambler.commons.auth.AuthenticationService">
        <property name="persistence">
            <ref local="simpleHibernatePersistence"/>
        </property>
    </bean>
</beans>
