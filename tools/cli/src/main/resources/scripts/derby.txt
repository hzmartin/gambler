connect 'jdbc:derby:/tmp/clidb;create=true';

drop table ENCRYPTED_PASSWORD;
CREATE TABLE ENCRYPTED_PASSWORD
    (
    ID INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY(START WITH 1) NOT NULL,
    UID VARCHAR(256),
    SITE VARCHAR(256),
    TYPE VARCHAR(64),
    PASSWD VARCHAR(1024));


    CREATE INDEX ENCRYPTED_PASSWORD_UID_SITE_IDX ON ENCRYPTED_PASSWORD (UID, SITE);
    
